# 2026-08-03

- 已将 M1 demo-first 路线与 Neon Vector Geometry 美术方向的文档更新提交到 `main`，提交为 `286ba23`（`docs: align M1 roadmap with demo-first visual direction`）。涉及 `project-source/ART_STYLE.md`、`ASSET_BRIEF_M1.md`、`PROJECT_CONTEXT.md`、`README.md`、`ROADMAP.md`。未提交 `.cindy-worktrees/` 相关工作树内容。
- 用户确立强制同步规则：项目代码或 `project-source/` 权威内容推送到 TapTap Maker Git 远程时，必须把同一提交历史推送到 GitHub `Yunlong2601/SHENTECHSTUDIO`，并分别核验两个远程分支的提交；一端成功不能代替另一端成功。
- 已将上述规则写入 `project-source/PROJECT_CONTEXT.md` 和 `project-source/README.md`，提交 `1c163ed`。因 GitHub `main` 与 Maker 历史无共同祖先，为避免覆盖 GitHub 现有 6 个提交，采用 GitHub 专用镜像分支 `taptap-main`。合并 Maker 自动生成的 i18n 同步提交后，最终提交为 `e988486`；已分别核验 TapTap Maker `main` 与 GitHub `taptap-main` 均指向 `e988486966827c5a1c734baff4c291d259c127d3`。
- 用户要求 `push to main`：将 GitHub `main`（旧 6 个提交）与 TapTap Maker `main`（48 个提交）做非破坏性 `--allow-unrelated-histories` 合并，4 个 add/add 冲突以当前 Maker 项目版本为准解决，旧 Godot 原型文件不重新加入。合并提交 `8072984`。已成功推送到三个目标分支并逐一核验 SHA 全部一致：TapTap Maker `main` = GitHub `main` = GitHub `taptap-main` = `80729848ce55467a65454af20828bd5b76cdfd35`。双端同步任务全部完成。
- 用户确立最终远程层级规则：**TapTap Maker `main` 是 base / source of truth，GitHub `main` 是 replica，必须始终镜像 TapTap Maker `main`。** 每次更新先推 TapTap Maker `main`，再推 GitHub `main`；两端各自核验。如果两端分歧，以 TapTap Maker `main` 为准，GitHub `main` 对齐它。已写入 `project-source/PROJECT_CONTEXT.md` §8 和 §10、`project-source/README.md` 规则 5，提交 `adbcd38`，已推送并核验三个远程分支全部指向 `adbcd38d5db0e72111f9fb2a8c3a915b7a9256e3`。
- 完成 M1 Demo Build 三项核心功能，提交 `03a9a5b`，已推送并核验 TapTap Maker `main` = GitHub `main` = GitHub `taptap-main` = `03a9a5b6e6e59791f4803567e77eb752ab4fbde4`。变更内容：
  1. **Neon Vector Geometry 视觉打磨**：所有实体（玩家、敌人、拾取物、弹幕、模块效果）改用渐变背景、按类型区分的几何形状和发光边框；竞技场背景增加径向光环；HUD 卡片和所有菜单面板使用 refined gradient panels。
  2. **Boss 遭遇战（第 6 波）**：Core Breaker 原型 Boss，带径向渐变六核心视觉、 telegraphed pulse attack、周期性 minion spawn、HUD Boss 血条。所有模块范围攻击（pulse、mines）也能伤害 Boss。Boss 死亡触发胜利。
  3. **变现占位界面**：主菜单可进入 Workshop 工坊界面，展示 4 个外观皮肤（1 已解锁、3 锁定）和开发者支持面板占位。未接入支付 API。
  4. 其他：修复缺失的 `game.shell` i18n key，prototype 标签改为 "Demo Build"，Boss 击败原因与普通碰撞死亡区分。
- 完成 M1 收尾 + M2 启动，提交 `0be148a`，已推送并核验 TapTap Maker `main` = GitHub `main` = GitHub `taptap-main` = `0be148a022172b5d1ddc6db63c04d32d11e46dba`。变更内容：
  1. **M1 收尾**：修复竞技场光环居中（原来默认在 0,0）；增加胜利/失败区分（胜利显示绿色标题和边框）；增加运行遥测面板（承受伤害、最远波次、阵亡次数、完整升级路径）；将伤害数字上限从 28 降至 18 提升 HUD 可读性；更新过时的 `i18n/en.json`（原来停留在 Prototype 03）。
  2. **M2 新内容**：新增 Splitter 分裂体敌人（绿色渐变，死亡后分裂为 2 个碎片）；新增 Shooter 射击体敌人（紫色渐变，保持距离并向玩家发射弹幕）；新增敌方弹幕系统和碰撞检测；新增 Glitch 故障修正器（第 7+ 波，实体随机位移）；游戏从 6 波扩展到 8 波，Boss 在第 8 波；更新 `kind_for_id` 使第 5 波起出现分裂体和射击体。
  3. **路线图**：M1 标记为完成，M2 标记为活跃；更新当前状态和工作队列。
- 用户简化远程同步规则：**只需推送到 TapTap Maker `main` 和 GitHub `main` 两个远程**，不再推送 GitHub `taptap-main` 分支。已更新 MEMORY.md 反映此变更。
- 已从 GitHub `Yunlong2601/SHENTECHSTUDIO` 删除 `taptap-main` 分支（`git push github --delete taptap-main`），消除混淆。`github` remote 已重新配置供后续推送使用。
- 完成 M2 Content Density PRD 全流程（产品战略团队 5 成员协作）。团队 `product-m2-content` 调度了 user-researcher、competitive-analyst、data-analyst（并行研究）→ requirement-analyst（PRD 撰写）→ roadmap-planner（时间线估算）。最终 PRD 落盘至 `deliverables/product-strategy/prd-m2-content-density-2026-08-03.md`，包含 15 项需求（P0×6 / P1×5 / P2×4），三阶段实施计划（~3.5 周 P0+P1 目标）。
- 关键设计决策（终值）：中 Boss W4 HP=60 / 终局 Boss W8 HP=300（100+wave×25）四阶段 DPS 检查 / Glitch 帧率独立 Poisson λ≈0.18/s 腐蚀叠加 Modifier（不替换）/ Splitter W3 引入 / Shooter W5 引入 / 加权生成分布 / 动态敌人上限（普通30/Glitch25）/ 碎片上限8-10。
- 用户确立新工作模式：方向明（主理人）负责项目总控和周任务拆解，用户用 CodeBuddy 执行开发，完成后报告回审。第一份周任务包（Week 1: R-02/R-03/R-05/R-06）即将下发。
- 用户切换开发工具：从 CodeBuddy 切换到 VS Code Copilot。已审计 CodeBuddy 完成情况：R-05 完整完成（碎片上限10），R-02 部分完成（加权随机已实现但波次门控错误——Splitter 应 W3 非 W5，Shooter 应 W5 非 W1，W1-2 应只有 Chaser+Skimmer），R-03 部分完成（Glitch 叠加层概念正确但缺少 Poisson 进程、Modifier 腐蚀、闪烁视觉、脉冲周期），R-06 仅完成 30%（maxEnemies_ 字段存在但从未从 24 动态更新）。
- 已产出一个月任务包给 VS Code Copilot，落盘至 `deliverables/product-strategy/month-task-package-vscode-2026-08-03.md`。四周计划：W1 修复 Phase 1 缺口（R-02/R-03/R-06），W2 中 Boss 遭遇战（R-01），W3 终局 Boss 升级 + 波次调优（R-04/R-07/R-08），W4 UI 反馈层（R-09/R-10/R-11）。
